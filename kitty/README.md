## Stow

Must be stowed with `--no-folding` to prevent `kitten themes` from writing generated files into the repo:

```bash
stow --no-folding kitty
```

After stowing, set up your theme:

```bash
kitten themes
```
