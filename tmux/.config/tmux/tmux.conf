# Change prefix
unbind C-b
set -g prefix M-';'
bind M-';' send-prefix

# Terminal settings
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-kitty:RGB"

# Better editor behavior
set -g focus-events on

# Mouse support (handles scrollback/copy-mode, avoids double scrollback)
set -g mouse on

# Renumber all windows when any window is closed
set -g renumber-windows on

# Use system clipboard
set -g set-clipboard on

setw -g mode-keys vi

# Start indexs from 1
set -g base-index 1
set -g pane-base-index 1

# Copy Mode
unbind [
bind s copy-mode

# Splits
unbind '%'
unbind '\'
bind v split-window -h
bind h split-window -v

# Pane Navigation
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Pane Resizing
bind -n M-H resize-pane -L 5
bind -n M-J resize-pane -D 5
bind -n M-K resize-pane -U 5
bind -n M-L resize-pane -R 5

# Using alt + tab number to move between tabs
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Status bar position
set -g status-position top

# Plugins Using TPM
set -g @plugin 'tmux-plugins/tpm'

# Dracula Theme
set -g @plugin 'dracula/tmux'
set -g @dracula-plugins "git"

run '~/.config/tmux/plugins/tpm/tpm'

# Pane Borders (matching sway window borders - after TPM to override theme)
set -g pane-border-style fg=#11111b
set -g pane-active-border-style fg=#ff9e64

